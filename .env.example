# Copy this file to ".env" and fill in your own values.
# Never commit the real ".env" file.

# --- Database ---
# These 3 are used by docker-compose to create the Postgres container.
# Keep them in sync with the credentials inside DATABASE_URL below.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ai_content_assistant

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_content_assistant

# --- JWT auth ---
JWT_SECRET_KEY=replace-with-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# --- LLM (Google Gemini) ---
# Get a key from https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# --- Rate limiting ---
# Max number of AI requests a single user can make per day
DAILY_REQUEST_LIMIT=20
